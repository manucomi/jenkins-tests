import { Meta, Story } from '@storybook/addon-docs';
import FontHelper from '../../../../../story-helpers/FontHelper/FontHelper';
import '../roboto-slab.scss';

<Meta
    title="Fonts/Roboto-Slab/Overview"
    argTypes={{
        fontFamily: {
            table: { disable: true },
        },
        weights: {
            table: { disable: true },
        },
        italic: {
            table: { disable: true },
        },
        size: {
            control: 'select',
            options: ['12px', '16px', '20px', '24px', '32px', '40px', '48px'],
        },
    }}
/>

# Roboto Slab

Roboto Slab features friendly and open curves,
as it has a mechanical skeleton and the forms are largely geometric.

## Import Font

Import the Roboto Slab font styles into one of your SASS modules.

```css
// _fonts.scss

@import 'mfe-core/typography/roboto-slab.css';
```

## Use font family

```css
.element {
    font-family: 'Roboto Slab';
}
```

{/* eslint-disable react/prop-types */}

export function Template(args) {
    const { weights, size, text, fontFamily, italic } = args;
    return (
        <FontHelper
            text={text}
            size={size}
            fontFamily={fontFamily}
            weights={weights}
            italic={italic}
        />
    );
}

<Story
    name="Overview"
    args={{
        text: 'The quick brown fox jumps over the lazy dog',
        size: '40px',
        fontFamily: 'Roboto Slab',
        weights: [200, 400, 700],
        italic: false,
    }}
>
    {Template.bind({})}
</Story>
