import { Meta, Story } from '@storybook/addon-docs';
import FontHelper from '../../../../../story-helpers/FontHelper/FontHelper';
import '../roboto-slab-thin.scss';

<Meta
    title="Fonts/Roboto-Slab-Thin/Overview"
    argTypes={{
        fontFamily: {
            table: { disable: true },
        },
        weights: {
            table: { disable: true },
        },
        italic: {
            table: { disable: true },
        },
        size: {
            control: 'select',
            options: ['12px', '16px', '20px', '24px', '32px', '40px', '48px'],
        },
    }}
/>

# Roboto Slab Thin

Variation of the Roboto Slab font.

## Import Font

Import the Roboto Slab Thin font styles into one of your SASS modules.

```css
// _fonts.scss

@import 'mfe-core/typography/roboto-slab-thin.css';
```

## Use font family

```css
.element {
    font-family: 'Roboto Slab Thin';
}
```

export function Template(args) {
    const { weights, size, text, fontFamily, italic } = args;
    return (
        <FontHelper
            text={text}
            size={size}
            fontFamily={fontFamily}
            weights={weights}
            italic={italic}
        />
    );
}

<Story
    name="Overview"
    args={{
        text: 'The quick brown fox jumps over the lazy dog',
        size: '40px',
        fontFamily: 'Roboto Slab Thin',
        weights: [200],
        italic: false,
    }}
>
    {Template.bind({})}
</Story>
