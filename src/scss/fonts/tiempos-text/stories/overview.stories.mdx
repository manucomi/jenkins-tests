import { Meta, Story } from '@storybook/addon-docs';
import FontHelper from '../../../../../story-helpers/FontHelper/FontHelper';
import '../tiempos-text.scss';

<Meta
    title="Fonts/Tiempos-Text/Overview"
    argTypes={{
        fontFamily: {
            table: { disable: true },
        },
        weights: {
            table: { disable: true },
        },
        italic: {
            table: { disable: true },
        },
        size: {
            control: 'select',
            options: ['12px', '16px', '20px', '24px', '32px', '40px', '48px'],
        },
    }}
/>

# Tiempos Text

Tiempos Text is a serif typeface mainly used for editorial typography.

## Import Font

Import the Tiempos Text font styles into one of your SASS modules.

```css
// _fonts.scss

@import 'mfe-core/typography/tiempos-text.css';
```

## Use font family

```css
.element {
    font-family: 'Tiempos Text';
}
```

export function Template(args) {
    const { weights, size, text, fontFamily, italic } = args;
    return (
        <FontHelper
            text={text}
            size={size}
            fontFamily={fontFamily}
            weights={weights}
            italic={italic}
        />
    );
}

<Story
    name="Overview"
    args={{
        text: 'The quick brown fox jumps over the lazy dog',
        size: '40px',
        fontFamily: 'Tiempos Text',
        weights: [200, 400, 500, 600, 700],
    }}
>
    {Template.bind({})}
</Story>
