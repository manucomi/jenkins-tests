import { Meta, Story } from '@storybook/addon-docs';
import FontHelper from '../../../../../story-helpers/FontHelper/FontHelper';
import '../saol-display.scss';

<Meta
    title="Fonts/Saol-Display/Overview"
    argTypes={{
        fontFamily: {
            table: { disable: true },
        },
        weights: {
            table: { disable: true },
        },
        italic: {
            table: { disable: true },
        },
        size: {
            control: 'select',
            options: ['12px', '16px', '20px', '24px', '32px', '40px', '48px'],
        },
    }}
/>

# Saol Display

Saol Display is the display version of the Saol family.
The design features razor-sharp serifs, high stroke contrast,
a large x-height and short descenders.

## Import Font

Import the Saol Display font styles into one of your SASS modules.

```css
// _fonts.scss

@import 'mfe-core/typography/saol-display.css';
```

## Use font family

```css
.element {
    font-family: 'Saol Display';
}
```

export function Template(args) {
    const { weights, size, text, fontFamily } = args;
    return (
        <FontHelper
            text={text}
            size={size}
            fontFamily={fontFamily}
            weights={weights}
        />
    );
}

<Story
    name="Overview"
    args={{
        text: 'The quick brown fox jumps over the lazy dog',
        size: '40px',
        fontFamily: 'Saol Display',
        weights: [200, 400, 500, 600, 700],
    }}
>
    {Template.bind({})}
</Story>
