/**
 * This mock is used to avoid syntax errors from importing images.
 * Tests don't run webpack first, so illegal imports will cause
 * the tests to fail.
 */
const image = '';
export default image;
