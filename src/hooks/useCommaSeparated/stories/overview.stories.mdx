import { Meta } from '@storybook/addon-docs';
import Table from '../../../../story-helpers/PropsTableHelper.mdx';

<Meta title="Hooks/useCommaSeparated/Overview" />

# useCommaSeparated

This hook is a memoized version of the utility function `commaSeparated`.

## Parameters

<Table
    propsList={[
        {
            name: 'arr',
            type: 'array&#60;string&#62;',
            description: 'The array of strings to format.',
        },
        {
            name: 'finalSeparator',
            type: 'string',
            description:
                'The separator to use before the final item in the list.',
            defaultValue: 'and',
        },
    ]}
/>

## Use Example

```js
import useCommaSeparated from 'mfe-core/hooks/useCommaSeparated';

function App() {
    const list = ['Kathy', 'James', 'Sharon'];
    const separatedString = useToCommaSeparated(list);

    console.log(separatedString); // Kathy, James, and Sharon

    return <></>;
}
```
