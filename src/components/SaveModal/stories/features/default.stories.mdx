import { Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import SaveModal from 'Components/SaveModal/SaveModal';
import { Template } from '../playground.stories.mdx';

# Default Alignment

By default the `SaveModal` is left aligned.

<Meta
    title="Components/SaveModal/Features"
    component={SaveModal}
    argTypes={{
        dataPoints: {
            table: { disable: true },
        },
        onClose: {
            table: { disable: true },
        },
        userEmailAddress: {
            table: { disable: true },
        },
    }}
/>

<Story
    name="Default"
    args={{
        dataPoints: {},
        userEmailAddress: '',
        onClose: action('onClose handler'),
    }}
>
    {Template.bind({})}
</Story>
