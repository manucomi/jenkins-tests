import { Meta, Canvas, Story } from '@storybook/addon-docs';
import Anchor from 'Components/Anchor/Anchor';
import '../../../../scss/fonts/gt-america/gt-america.scss';

# Anchor with label tag

Consumer application can also provide a `label` tag.

<Meta
    title="Components/Anchor/Features"
    component={Anchor}
    argTypes={{
        children: { table: { disable: true } },
        className: { table: { disable: true } },
        href: { table: { disable: true } },
        onClick: { table: { disable: true } },
        tabIndex: { table: { disable: true } },
        newTab: { table: { disable: true } },
    }}
/>

export function Template(args) {
    /* eslint-disable no-unused-vars */
    const { href, children, label, newTab, onClick, tabIndex, ref } = args;
    return (
        <Anchor
            href={href}
            label={label}
            newTab={newTab}
            onClick={onClick}
            tabIndex={tabIndex}
        >
            {children}
        </Anchor>
    );
}

<Canvas>
    <Story
        name="Labeled Anchor"
        args={{
            children: 'Click Link!',
            href: 'https://hbr.org',
            label: 'Go to Harvard Bussines Review',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
