import { Meta, Story, Canvas } from '@storybook/addon-docs';
import InterstitialModal from 'Components/InterstitialModal/InterstitialModal';
import { Template } from '../playground.stories.mdx';

# Default InterstitialModal

By default the `InterstitialModal` targets a given Ad for Mobile and Desktop adSizes.

**Note:**
For testing purposes, we are targeting a specific dummy Ad, in order to perform the
visual diff tests.

<Meta
    title="Components/InterstitialModal/Features"
    component={InterstitialModal}
    argTypes={{
        desktopAdProps: { table: { disable: true } },
        mobileAdProps: { table: { disable: true } },
    }}
/>

<Canvas>
    <Story
        name="Default"
        args={{
            desktopAdProps: {
                path: '/34363400/TEST_Single-PartnerCenter_230x230',
                size: [[230, 230]],
                targets: { position: '11' },
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
