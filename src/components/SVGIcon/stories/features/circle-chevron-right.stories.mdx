import { Meta, Story } from '@storybook/addon-docs';
import SVGIcon from 'Components/SVGIcon/SVGIcon';
import { Template } from '../playground.stories.mdx';

# Circle Chevron Right

Displays a _Circle Chevron Right_ HBR icon.

<Meta
    title="Components/SVGIcon/Features"
    component={SVGIcon}
    argTypes={{
        className: {
            table: { disable: true },
        },
        label: {
            table: { disable: true },
        },
        variant: {
            options: Object.values(SVGIcon.variants),
            control: { type: 'radio' },
        },
    }}
/>

<Story
    name="Circle Chevron Right"
    args={{
        variant: SVGIcon.variants.CIRCLE_CHEVRON_RIGHT,
    }}
>
    {Template.bind({})}
</Story>
