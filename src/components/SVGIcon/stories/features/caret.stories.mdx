import { Meta, Story } from '@storybook/addon-docs';
import SVGIcon from 'Components/SVGIcon/SVGIcon';
import { Template } from '../playground.stories.mdx';

# Caret

Displays a _Caret Up_ icon.

<Meta
    title="Components/SVGIcon/Features"
    component={SVGIcon}
    argTypes={{
        className: {
            table: { disable: true },
        },
        label: {
            table: { disable: true },
        },
        variant: {
            options: Object.values(SVGIcon.variants),
            control: { type: 'radio' },
        },
    }}
/>

<Story
    name="Caret"
    args={{
        variant: SVGIcon.variants.CARET,
    }}
>
    {Template.bind({})}
</Story>
