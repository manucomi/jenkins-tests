import { Meta, Story } from '@storybook/addon-docs';
import SVGIcon from 'Components/SVGIcon/SVGIcon';
import { Template } from '../playground.stories.mdx';

# Arrow Right Nav

Displays a _Arrow Right Nav_ icon.

<Meta
    title="Components/SVGIcon/Features"
    component={SVGIcon}
    argTypes={{
        className: {
            table: { disable: true },
        },
        label: {
            table: { disable: true },
        },
        variant: {
            options: Object.values(SVGIcon.variants),
            control: { type: 'radio' },
        },
    }}
/>

<Story
    name="Arrow Right Nav"
    args={{
        variant: SVGIcon.variants.ARROW_RIGHT_NAV,
    }}
>
    {Template.bind({})}
</Story>
