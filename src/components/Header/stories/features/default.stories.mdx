import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Header from 'Components/Header/Header';

<Meta title="Components/Header/Features" component={Header} />

# Default

This component receives no props so there are no controls. Services are mocked
to make visible features like the cart and auto suggest search.

export function Template() {
    return <Header />;
}

<Canvas>
    <Story name="Default">{Template.bind({})}</Story>
</Canvas>
