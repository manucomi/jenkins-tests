import { Meta } from '@storybook/addon-docs';
import Loader from 'Components/Loader/Loader';
import PropsTable from '../../../../story-helpers/PropsTableHelper.mdx';

<Meta title="Components/Loader/Overview" component={Loader} />

# Loader

export const propsList = [
    {
        name: 'className',
        type: 'string',
        defaultValue: 'EMPTY',
        description: 'Extends the styles applied to the component.',
    },
];

<PropsTable propsList={propsList} />

## Use Example

```jsx
import Loader from 'mfe-core/ui/Loader';

return <Loader className={styles.loading} />;
```
