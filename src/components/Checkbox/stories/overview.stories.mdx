import { Meta } from '@storybook/addon-docs';
import Checkbox from 'Components/Checkbox/Checkbox';
import PropsTable from '../../../../story-helpers/PropsTableHelper.mdx';

<Meta title="Components/Checkbox/Overview" component={Checkbox} />

# Checkbox

## Props

export const propsList = [
    {
        name: 'checkboxClassName',
        type: 'string',
        defaultValue: 'EMPTY',
        description: 'Checkbox styling variation.',
    },
    {
        name: 'isChecked',
        type: 'bool',
        description:
            'Defines whether the checkbox is initially rendered as checked.',
    },
    {
        name: 'label',
        type: 'string',
        description: 'Text to be rendered alongside checkbox.',
    },
    {
        name: 'labelClassName',
        type: 'string',
        defaultValue: 'EMPTY',
        description: 'Label styling variation.',
    },
    {
        name: 'onChange',
        type: 'func',
        description: 'Function handler for checking/unchecking changes.',
    },
    {
        name: 'afterLabel',
        type: 'bool',
        defaultValue: 'false',
        description:
            'Defines whether the label should be rendered before or after checkbox.',
    },
    {
        name: 'disabled',
        type: 'bool',
        defaultValue: 'false',
        description: 'Disabled checkbox from changing from its initial state.',
    },
    {
        name: 'data-qa',
        type: 'string',
        defaultValue: 'null',
        description: 'Name field to be set during QA testing.',
    },
];

<PropsTable propsList={propsList} />

## Use example

```jsx
import Checkbox from 'mfe-core/ui/CheckBox';

const [isChecked, setIsChecked] = useState(false);
const [isDisabled, setIsDisabled] = useState(false);
const handleChange = () => setIsChecked(!isChecked);

return (
    <Checkbox
        checkboxClassName={styles.checkbox}
        labelClassName={styles.label}
        isChecked={isChecked}
        onChange={handleChange}
        label="I Agree"
        disabled={isDisabled}
        data-qa="automation-test-selector"
        afterLabel
    />
);
```
