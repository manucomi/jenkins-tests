import { Meta, Canvas, Story } from '@storybook/addon-docs';
import Button from 'Components/Button/Button';

<Meta
    title="Components/Button/Playground"
    component={Button}
    argTypes={{
        variant: {
            options: Object.values(Button.variants),
            control: { type: 'radio' },
        },
        children: {
            table: { disable: true },
        },
    }}
/>

# Playground

Freely modify controls for this component.

export function Template(args) {
    const {
        children,
        className,
        disabled,
        label,
        onClick,
        submit,
        variant,
        id,
        ariaExpanded,
    } = args;
    return (
        <div>
            <Button
                className={className}
                disabled={disabled}
                label={label}
                onClick={onClick}
                submit={submit}
                variant={variant}
                id={id}
                ariaExpanded={ariaExpanded}
            >
                {children}
            </Button>
        </div>
    );
}

<Canvas>
    <Story
        name="Playground"
        args={{
            children: 'Click me!',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
