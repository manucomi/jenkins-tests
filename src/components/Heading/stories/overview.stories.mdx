import { Meta } from '@storybook/addon-docs';
import Heading from 'Components/Heading/Heading';
import PropsTable from '../../../../story-helpers/PropsTableHelper.mdx';

<Meta title="Components/Heading/Overview" component={Heading} />

# Heading

## Props

export const propsList = [
    {
        name: 'level',
        type: 'number',
        description: `Controls the six levels of section heading: <br/>
            <code>1</code> <br/>
          | <code>2</code> <br/>
          | <code>3</code> <br/>
          | <code>4</code> <br/>
          | <code>5</code> <br/>
          | <code>6</code>`,
    },
    {
        name: 'className',
        type: 'string',
        defaultValue: 'EMPTY',
        description: 'Extends the styles applied to the component.',
    },
];

<PropsTable propsList={propsList} />

## Use example

```jsx
import Heading from 'mfe-core/ui/Heading';

return (
    <Heading level={1} className={styles['title-inline']}>
        In the News
    </Heading>
);
```
