import { Meta, Story } from '@storybook/addon-docs';
import HeadingComponent from 'Components/Heading/Heading';
import '../../../scss/fonts/gt-america/gt-america.scss';

# Playground

Change the controls as you wish, to see how the component behaves.

<Meta
    title="Components/Heading"
    component={HeadingComponent}
    argTypes={{
        level: {
            options: [1, 2, 3, 4, 5, 6],
            control: { type: 'radio' },
        },
        children: {
            table: { disable: true },
        },
    }}
/>

export function Template(args) {
    const { level } = args;
    return (
        <HeadingComponent level={level}>{`H${level} heading`}</HeadingComponent>
    );
}

<Story
    name="Playground"
    args={{
        level: 1,
    }}
>
    {Template.bind({})}
</Story>
