import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Accordion from 'Components/Accordion/Accordion';
import styles from '../stories.module.scss';

# Playground

Change the controls as you wish, to see how the component behaves.

<Meta
    title="Components/Accordion"
    component={Accordion}
    argTypes={{
        children: { table: { disable: true } },
    }}
/>

export function Template(args) {
    const { children, className, title, isInitiallyOpen } = args;
    return (
        <div className={styles.container}>
            <Accordion
                title={title}
                isInitiallyOpen={isInitiallyOpen}
                className={className}
            >
                {children}
            </Accordion>
        </div>
    );
}

export const mockItems = ['Item Number 1', 'Item Number 2', 'Item Number 3'];

<Canvas>
    <Story
        name="Playground"
        args={{
            title: 'Accordion',
            children: mockItems.map((item) => (
                <div key={item} className={styles['accordion-item']}>
                    {item}
                </div>
            )),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
