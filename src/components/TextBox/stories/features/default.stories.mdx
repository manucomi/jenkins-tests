import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import TextBox from 'Components/TextBox/TextBox';
import { Template } from '../playground.stories.mdx';
import styles from '../stories.module.scss';

<Meta
    title="Components/TextBox/Features"
    component={TextBox}
    argTypes={{
        ariaLabel: {
            table: { disable: true },
        },
        textBoxClassName: {
            table: { disable: true },
        },
        labelClassName: {
            table: { disable: true },
        },
        'data-qa': {
            table: { disable: true },
        },
        name: {
            table: { disable: true },
        },
        onChange: {
            table: { disable: true },
        },
        required: {
            table: { disable: true },
        },
        disabled: {
            table: { disable: true },
        },
        value: {
            table: { disable: true },
        },
    }}
/>

# Default TextBox

By Default the `TextBox` renders the component with the placeholder set to an empty string.

<Canvas>
    <Story
        name="Default"
        args={{
            label: 'Enter some text',
            placeholder: '',
            name: 'text-input',
            onChange: action('typed'),
            labelClassName: styles.label,
            textBoxClassName: styles.textarea,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
