import { Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import AdSlot from 'Components/AdSlot/AdSlot';
import { Template } from '../playground.stories.mdx';

# Default

Displays a squared-shaped adslot used in places such as the Partner Center.

<Meta
    title="Components/AdSlot/Features"
    component={AdSlot}
    argTypes={{
        className: {
            table: { disable: true },
        },
        onSlotRender: {
            table: { disable: true },
        },
    }}
/>

export const beforeTests = async () => {
    // eslint-disable-next-line no-promise-executor-return
    await new Promise((resolve) => setTimeout(resolve, 600));
};

<Story
    name="Default"
    args={{
        path: '/34363400/TEST_Single-PartnerCenter_230x230',
        size: [[230, 230]],
        targets: { position: '11' },
        onSlotRender: action('adslot loaded'),
    }}
    parameters={{
        custom: { beforeA11y: beforeTests, beforeScreenshot: beforeTests },
    }}
>
    {Template.bind({})}
</Story>
