import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import TextInput from 'Components/TextInput/TextInput';
import { Template } from '../playground.stories.mdx';

<Meta
    title="Components/TextInput/Features"
    component={TextInput}
    argTypes={{
        ariaLabel: {
            table: { disable: true },
        },
        textInputClassName: {
            table: { disable: true },
        },
        labelClassName: {
            table: { disable: true },
        },
        'data-qa': {
            table: { disable: true },
        },
        label: {
            table: { disable: true },
        },
        name: {
            table: { disable: true },
        },
        onChange: {
            table: { disable: true },
        },
        placeholder: {
            table: { disable: true },
        },
        required: {
            table: { disable: true },
        },
        variant: {
            table: { disable: true },
        },
        value: {
            table: { disable: true },
        },
    }}
/>

# Readonly Type

Text input with `disabled` property set to `true`.

<Canvas>
    <Story
        name="ReadOnly"
        args={{
            label: 'Read Only Input',
            placeholder: 'Placeholder',
            name: 'text-input',
            variant: TextInput.variants.SEARCH,
            onChange: action('input changed'),
            disabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
