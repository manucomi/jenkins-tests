import { Meta } from '@storybook/addon-docs';
import AuthModal from 'Components/AuthModal/AuthModal';
import PropsTable from '../../../../story-helpers/PropsTableHelper.mdx';
import Table from '../../../../story-helpers/TableHelper.mdx';

<Meta title="Components/AuthModal/Overview" component={AuthModal} />

# AuthModal

## Props

<PropsTable
    propsList={[
        {
            name: 'selectedView',
            type: 'string',
            defaultValue: 'SIGN_IN',
            description:
                'Controls the form we want to show when the modal opens. See <a href="#views" target="_self">Views</a> section for more information.',
        },
        {
            name: 'onClose',
            type: 'func',
            description:
                'Triggers the event handler when the <code>AuthModal</code> is closed.',
        },
    ]}
/>

<div id="views" />

## Views

The views are added as a property of the `AuthModal` component and are accessible as `AuthModal.views`.

<Table
    headings={['Property', 'Description']}
    body={[
        [
            '<code>AuthModal.views.SIGN_IN</code>',
            'Shows the Sign In form when the modal is open.',
        ],
        [
            '<code>AuthModal.views.REGISTER</code>',
            'Shows the Registration form when the modal is open.',
        ],
        [
            '<code>AuthModal.views.FORGOT_PASSWORD</code>',
            'Shows the Forgot Passsord form when the modal is open.',
        ],
    ]}
/>

## Use example

```jsx
import AuthModal from 'mfe-core/ui/AuthModal';

const [isAuthOpen, setIsAuthOpen] = useState(false);
const handleClose = () => setIsAuthOpen(false);
const handleOpen = () => setIsAuthOpen(true);
return (
    <>
        <button onClick={handleOpen} type="button">
            Register
        </button>
        {isAuthOpen && (
            <AuthModal
                selectedView={AuthModal.views.REGISTER}
                onClose={handleClose}
            />
        )}
    </>
);
```
