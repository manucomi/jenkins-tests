import { Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import Modal from 'Components/Modal/Modal';
import styles from '../stories.module.scss';

# Default Alignment

By default the `Modal` is left aligned.

<Meta
    title="Components/Modal/Features"
    component={Modal}
    argTypes={{
        children: {
            table: { disable: true },
        },
        label: {
            table: { disable: true },
        },
        onClose: {
            table: { disable: true },
        },
        backdropClassName: {
            table: { disable: true },
        },
        modalClassName: {
            table: { disable: true },
        },
        isFocusTrapped: {
            table: { disable: true },
        },
        container: {
            table: { disable: true },
        },
    }}
/>

export function Template(args) {
    const {
        label,
        onClose,
        backdropClassName,
        modalClassName,
        isFocusTrapped,
        isCentered,
    } = args;
    return (
        <div style={{ height: '500px' }}>
            <Modal
                label={label}
                onClose={onClose}
                backdropClassName={backdropClassName}
                modalClassName={modalClassName}
                isFocusTrapped={isFocusTrapped}
                isCentered={isCentered}
            >
                <button
                    type="button"
                    className={styles['btn-close']}
                    onClick={onClose}
                >
                    X
                </button>
                <h3>Modal</h3>
                <p className={styles.text}>
                    Type `Esc` or click outside the modal to close.
                </p>
            </Modal>
        </div>
    );
}

<Story
    name="Default"
    args={{
        children: null,
        label: 'Welcome Dialog',
        onClose: action('onClose handler'),
        backdropClassName: '',
        modalClassName: styles.modal,
        isFocusTrapped: false,
        isCentered: false,
    }}
>
    {Template.bind({})}
</Story>
